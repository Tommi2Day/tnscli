app: "tnscli"
info: true
tns_admin: testdata

ldap:
 base: "dc=oracle,dc=local"
 oraclectx: "dc=oracle,dc=local"
 host: "localhost"
 port: 2389
 tls: false
 insecure: false
 binddn: "cn=admin,dc=oracle,dc=local"
 #bindpassword: "admin"
 timeout: 20
